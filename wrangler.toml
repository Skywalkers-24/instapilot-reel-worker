name = "instapilot-reel-publish-cron"
main = "src/index.js"
compatibility_date = "2026-08-28"

[triggers]
# Trigger every 15 minutes. The backend's post_interval_minutes setting remains
# authoritative, so missed or manual extra calls cannot over-post.
crons = ["*/15 * * * *"]
